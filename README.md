# 📋 Queue Management System (React)

A simple Queue Management System built with **React** that allows users to manage customers efficiently by adding them to a queue, updating their status, and removing them when needed.

---

## 🚀 Features

- ✅ Add customers to the queue
- 🔄 Update customer status (Pending, Served, etc.)
- ❌ Remove customers from the queue
- ⚡ Real-time UI updates using React state
- 🆔 Unique customer IDs using `Date.now()`

---

## 🛠️ Technologies Used

- React
- JavaScript (ES6+)
- React Hooks (`useState`)
- CSS

---

## 📂 Project Structure

```
    src/
    │
    ├── App.js
    ├── components/
    │       ├── QueueForm.js
    │       └── QueueDisplay.js
    └── App.css
```
---

▶️ Getting Started

1. Clone the repository:

   ```bash
    git clone https://github.com/techparth7/queue-management-system.git
   ```


2️⃣ Navigate to the project directory

    ```bash
        cd queue-management-system
    ```

3️⃣ Install dependencies

    ```bash
    npm install
    ```

4️⃣ Run the application

    ```bash
    npm start
    ```

The app will be available at:

    ```bash
    http://localhost:5137
    ```
